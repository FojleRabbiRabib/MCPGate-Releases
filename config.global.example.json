// MCPGate global config example — copy to ~/.mcpgate/config.json and edit.
// Controls server-wide behaviour. Applied once at startup and on SIGHUP / hot-reload.
//
// Workspace configs (.mcpgate/config.json inside a project) are merged on top.
// Stateful sessions retain the validated workspace overlay while active and watch
// that config target for valid replacements; invalid edits preserve the previous
// last-known-good overlay. Command additions extend both execute_command and
// bridge/boost spawning while the two built-in base command lists remain distinct.
//
// Run `mcpgate config show` to inspect the fully-merged effective config.
// Run `mcpgate config validate` to verify this file without starting the server.
{
  "version": "1.0.0",

  // Keep MCPGate-owned tools available if the boost upstream cannot start, dies,
  // hangs, or returns malformed protocol data. Resource-policy failures still fail closed.
  "boost": { "degradeOnUpstreamFailure": true },

  // ── Runtime limits ────────────────────────────────────────────────────────
  "limits": {
    "maxSessions": 100,        // Max concurrent sessions across all types
    "maxFileSize": 10485760,   // Max readable/writable file size in bytes (10 MB)
    "outputLimit": 153600,     // Per-tool output truncation threshold in bytes (150 KB)
    "commandTimeout": 60000,   // Subprocess fallback and hard ceiling in milliseconds
    "idleTimeout": 600000,     // Bridge session idle timeout in milliseconds (10 min)
    "shutdownTimeout": 15000,  // Graceful-shutdown wait in milliseconds
    "maxBatchOperations": 15,  // Max operations per batch_execute call
    "batchOutputLimit": 921600, // Batch aggregate response byte cap (900 KB)

    // Process-tree memory budgets in bytes. Zero means unlimited. "auto" uses
    // hard platform enforcement when available and otherwise a monitored
    // fallback; "hard-required" rejects capped work without a hard backend;
    // "monitor" always uses the monitored backend.
    "executionMemory": {
      "enforcement": "auto",
      "maxSubprocess": 0,
      "maxSession": 0,
      "maxAggregate": 0
    },

    // Process-wide bounded fair scheduling for expensive setup and tool work.
    // maxQueueWait is milliseconds; all fields are global-only and hot-reloaded.
    "admission": {
      "toolConcurrency": 32,
      "sessionSetupConcurrency": 8,
      "maxQueued": 256,
      "maxQueuedPerScope": 32,
      "maxQueueWait": 30000
    }
  },

  // ── Claude Code memory ────────────────────────────────────────────────────
  // Dedicated memory operations manage Claude project instructions/rules and
  // MCPGate's .mcpgate/memory/ store. Users may symlink that directory to
  // Claude Code's project memory location when shared files are desired.
  // Generic filesystem tools do not gain memory write access. Writes occur only
  // through explicit dedicated memory create/update/move/delete operations.
  // Global hard ceilings are 1,000 files and 16 MiB total decoded content.
  "memory": {
    "mode": "read-write",     // disabled | read-only | read-write
    "maxFiles": 200,
    "maxFileSize": 65536,     // bytes per memory file
    "maxTotalSize": 4194304,  // aggregate bytes across all memory surfaces
    "maxSearchResults": 50,
    "maxSnippetBytes": 2048
  },

  // ── Security ──────────────────────────────────────────────────────────────
  "permissions": {
    // Path prefixes ALWAYS blocked regardless of workspace config.
    // Merged with hardcoded defaults (/etc, ~/.ssh, ~/.mcpgate, etc.).
    // Workspace configs cannot remove entries from this list.
    "deniedPaths": [],

    // Explicit global replacement for both command surfaces. When empty, the
    // bridge/boost defaults are bun, deno, node, npx, php, python, python3, uvx;
    // execute_command defaults are basename, dirname, head, pwd, tail, which.
    "allowedCommands": [],

    // Explicit additive command permissions. Global and workspace entries
    // extend both execute_command and bridge/boost spawning while preserving
    // the distinct built-in base lists. Add "uv" to permit command=uv.
    "additionalAllowedCommands": [],

    // Additional path prefixes the agent may read/write.
    // Both global and workspace configs may contribute; the union is used.
    "additionalAllowedPaths": [],

    // Set to false to disable execute_command globally.
    // Workspace configs may also toggle this.
    "enableCommandExecution": true,

    // Require read_file to be called on a file before write_file / edit_file
    // may modify it in the same session. Off by default.
    "requireReadBeforeWrite": false,

    // Per-command argument blocklists merged with hardcoded safe defaults.
    // Configured blocks apply to agent execution and bridge/boost spawning.
    // Added bridge/boost commands also receive generic-command hardcoded blocks.
    "commandArgBlocklist": {
      "git": ["--upload-pack", "--receive-pack"]
    }
  },

  // ── Spawn rate limiting ───────────────────────────────────────────────────
  // Token-bucket limiter applied per source IP to /sse and /mcp endpoints.
  "spawnRateLimit": {
    "requests": 600,   // Allowed spawn requests per window
    "window": "15m"    // Window duration (e.g. "1m", "15m", "1h")
  },

  // ── Tool activation ───────────────────────────────────────────────────────
  // IMPORTANT: "disabled" and "disabledTools" are two different fields.
  //
  //   tools.disabled      — suppresses entire CATEGORIES (e.g. "web", "git")
  //   tools.disabledTools — suppresses individual TOOL NAMES (e.g. "git_push")
  //
  // Putting individual tool names under "disabled" has no effect — they are
  // treated as unknown category names and silently ignored. Always use
  // "disabledTools" to suppress tools by name.
  "tools": {
    // "auto"   — activate categories inferred from project-type detection
    // "all"    — activate every tool category
    // "manual" — activate only the categories listed in "enabled"
    "mode": "auto",

    // Tool CATEGORIES to enable (only when mode = "manual").
    // Known values: android, batch, commands, filesystem, formatting, git,
    //               images, laravel, memory, ml, package_manager, project,
    //               tasks, testing, web
    "enabled": [],

    // Tool CATEGORIES to suppress even in "auto" or "all" mode.
    "disabled": [],

    // Override auto-detected project types.
    // Known values: go, node, python, rust, ruby, java, kotlin, flutter,
    //               react, vue, angular, php, dotnet, cpp, bash, generic
    "forceProjectTypes": [],

    // Individual TOOL NAMES to enable within their category.
    // When non-empty, only the listed tools are active inside their category.
    // Takes precedence over disabledTools for the same name.
    "enabledTools": [],

    // Individual TOOL NAMES to suppress even when their category is active.
    // Use this — NOT "disabled" — to remove specific tools by name.
    "disabledTools": [],

    // Per-tool invocation rate limits. Absent = unlimited for that tool.
    // Stateful agents keep session-local buckets. Stateless /mcp retains bounded
    // process-local buckets by authenticated principal + workspace across
    // independent requests and ?tools= ACL changes. Unchanged reloads preserve
    // consumed quota; a changed rate policy starts a fresh configured bucket.
    "rateLimit": {
      "web_search":      { "requests": 20, "per": "minute" },
      "execute_command": { "requests": 60, "per": "minute" }
    },

    // Subprocess execution budgets in milliseconds. Category entries are
    // defaults; individual tool entries win and may be higher or lower than
    // their category, but every value must remain at or below
    // limits.commandTimeout. Omit a name to inherit the next broader policy.
    // Valid category and tool names are listed in README.md.
    "executionTimeouts": {
      "categories": {
        "commands": 45000,
        "formatting": 20000,
        "testing": 45000
      },
      "tools": {
        "execute_command": 30000,
        "lint_code": 15000,
        "run_tests": 30000,
        "test_coverage": 40000
      }
    },

    // Optional per-category and per-tool subprocess memory ceilings in bytes.
    // An omitted name inherits limits.executionMemory.maxSubprocess. Zero means
    // unlimited only when the inherited process ceiling is also unlimited.
    "executionMemory": {
      "categories": {},
      "tools": {}
    },

    // Per-tool confirmation policy; never selected by client name or User-Agent.
    // Each value is:
    //   "auto"   — confirm invocations classified as high-consequence.
    //   "always" — confirm every invocation when the negotiated MCP client can
    //                complete a standard confirmation flow.
    //   "never"  — skip confirmation.
    // Form confirmation is emitted only when the client advertises form elicitation.
    // On 2026-07-28+ the SDK carries InputRequests/InputResponses through MRTR;
    // initialize-based clients use the same contract through the SDK bridge. Clients without form elicitation
    // continue without a server-side confirmation interaction; destructiveHint,
    // ACLs, path/command policy, and other authorization controls remain in force.
    // Standard confirmation uses a random single-use authorization bound to the
    // exact decoded invocation and immutable policy identity; approval executes
    // the captured action instead of accepting replacement arguments.
    // Only tools wired to confirmation policy honor these entries. Confirmable
    // actions include destructive filesystem/task operations, consequential Git
    // changes, runtime/project execution, dependency mutation, archive extraction,
    // Android mutations, and Laravel destructive execution.
    "confirm": {
      "delete_file": "auto"
    },

    // Whether a per-workspace .mcpgate/config.json may LOOSEN a confirmation
    // (e.g. set a destructive tool to "never"). When false (default) a workspace
    // may only TIGHTEN (raise a tool toward "always"); any loosening entry is
    // ignored — so an untrusted repo can never disarm a prompt on its own.
    // GLOBAL ONLY.
    "allowWorkspaceConfirmOverrides": false
  },

  // ── Auto-update ───────────────────────────────────────────────────────────
  "update": {
    "channel": "stable",        // "stable" or "beta"
    "autoCheck": true,          // Background check on server startup
    "checkIntervalHours": 24    // How often the background check re-runs (hours)
  },

  // ── Web search provider ───────────────────────────────────────────────────
  // API keys here are overridden by MCPGATE_BRAVE_API_KEY / MCPGATE_TAVILY_API_KEY
  // environment variables. Prefer env vars so keys stay off disk.
  "search": {
    // "auto"       — pick first provider with a key, fall back to DuckDuckGo
    // "brave"      — force Brave Search (requires braveApiKey or MCPGATE_BRAVE_API_KEY)
    // "tavily"     — force Tavily (requires tavilyApiKey or MCPGATE_TAVILY_API_KEY)
    // "duckduckgo" — force DuckDuckGo (no key required)
    "provider": "auto",
    "braveApiKey": "",
    "tavilyApiKey": ""
  },

  // ── CORS (browser origin allowlist) ───────────────────────────────────────
  // GLOBAL ONLY — a workspace .mcpgate/config.json cannot set this; a "cors"
  // block there is silently ignored.
  //
  // Origins permitted to call the API from a browser. Empty (the default)
  // rejects browser callers at preflight (no Access-Control-Allow-Origin).
  // Use a single "*" to opt into wildcard (downgraded to a reflected origin
  // when the request carries credentials). The CLI --cors-origin flag /
  // MCPGATE_CORS_ORIGIN env var overrides this list when set.
  //
  // Hot-reloaded on SIGHUP, file change, and the dashboard "Reload Config"
  // button — no restart needed.
  "cors": {
    "origins": []
  },

  // ── Temp spill retention ────────────────────────────────────────────────
  // GLOBAL ONLY — a workspace .mcpgate/config.json cannot set this; a "temp"
  // block there is silently ignored.
  //
  // Tool outputs larger than the per-tool / batch byte caps are spilled to
  // per-project files under ~/.mcpgate/tmp/<workspace-slug>/ so the LLM can
  // read the full output back. retentionMs is how long those spill files live
  // before the background janitor removes them, in milliseconds. Zero (the
  // default) means use the built-in default of one hour. A startup sweep also
  // clears the whole dir once between runs. Set a larger value to keep
  // oversized outputs retrievable for longer at the cost of disk use.
  //
  // Hot-reloaded on SIGHUP, file change, and the dashboard "Reload Config"
  // button — the janitor consults this on every sweep, so no restart is needed.
  "temp": {
    "retentionMs": 0
  }
}
